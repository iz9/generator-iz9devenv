<% if (isPrettier) { %>
    const fs = require('fs')
    const path = require('path')
    const prettierOptions = JSON.parse(
            fs.readFileSync(path.resolve(__dirname, '.prettierrc'), 'utf8'),
        )
<% } %>

module.exports = {
    'env': {
        'commonjs': true,
        'es2021': true,
        'node': true
    },
    'extends': [
        'eslint:recommended',
        <% if (isPrettier) { %>'prettier', <% } %>
    ],
    'plugins': [
        <% if (isPrettier) { %>'prettier', <% } %>
    ],
    'parserOptions': {
        'ecmaVersion': 12
    },
    'rules': {
        'indent': [
            'error',
            2
        ],
        'linebreak-style': [
            'error',
            'unix'
        ],
        'quotes': [
            'error',
            'single'
        ],
        'semi': [
            'error',
            'never'
        ],
        <% if (isPrettier) { %> 'prettier/prettier': ['error', prettierOptions], <% } %>
    }
}
